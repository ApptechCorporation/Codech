<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:fitsSystemWindows="true"
    android:orientation="horizontal">

    <!-- Painel lateral de arquivos (fixo ao lado do editor) -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_root"
        android:name="com.tyron.code.ui.file.tree.TreeFileManagerFragment"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:background="@drawable/nav_background"
        android:visibility="gone" />

    <!-- Divisor vertical entre o painel e o editor -->
    <View
        android:id="@+id/nav_divider"
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:background="?attr/colorOutline"
        android:visibility="gone" />

    <!-- Area principal: toolbar + editor -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/app_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layoutDirection="ltr"
            app:elevation="0dp">

            <com.tyron.actions.impl.ActionToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />
        </com.google.android.material.appbar.AppBarLayout>

        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/progressbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Este wrapper extra e necessario para que os insets de janela
             continuem sendo despachados para outras views -->
        <com.tyron.code.util.NoInsetFrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true"
            android:fitsSystemWindows="true">

            <androidx.fragment.app.FragmentContainerView
                android:id="@+id/root"
                android:name="com.tyron.code.ui.editor.EditorContainerFragment"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                tools:layout="@layout/editor_container_fragment" />
        </com.tyron.code.util.NoInsetFrameLayout>
    </LinearLayout>

</LinearLayout>
